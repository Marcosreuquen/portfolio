import styled, { css } from "styled-components";

export const First = styled.h2`
  font-family: var(--font-title);
  color: var(--strong-black);
  font-family: Space Grotesk;
  font-size: 38px;
  font-style: normal;
  font-weight: bold;
  text-align: left;
  margin: 0;
`;
